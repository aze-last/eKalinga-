<Window x:Class="AttendanceShiftingManagement.Views.CrewMainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d" 
        d:DesignHeight="600" d:DesignWidth="800"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{materialDesign:MaterialDesignFont}"
        WindowState="Maximized"
        Title="Attendance Management System - Crew">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="{DynamicResource PrimaryHueMidBrush}" materialDesign:ElevationAssist.Elevation="Dp4">
            <Grid Background="#FF0916E5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="16,32,16,32">
                    <materialDesign:PackIcon Kind="Account" Width="48" Height="48" Foreground="White" HorizontalAlignment="Center"/>
                    <TextBlock Text="CREW PORTAL" Foreground="White" HorizontalAlignment="Center" FontWeight="Bold" Margin="0,8,0,0"/>
                </StackPanel>

                <StackPanel Grid.Row="1">
                    <Button Style="{StaticResource MaterialDesignFlatButton}" Foreground="White" Height="50" Content="My Schedule" FontSize="16"/>
                    <Button Style="{StaticResource MaterialDesignFlatButton}" Foreground="White" Height="50" Content="Time Clock" FontSize="16"/>
                </StackPanel>

                <Button Grid.Row="2" Style="{StaticResource MaterialDesignFlatButton}" 
                        Foreground="White" Height="50" Margin="0,0,0,16" Click="Logout_Click">
                    <StackPanel Orientation="Horizontal" Width="160">
                        <materialDesign:PackIcon Kind="Logout" Margin="0,0,16,0"/>
                        <TextBlock Text="Logout"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <Grid Grid.Column="1">
            <ContentControl Content="{Binding CurrentView}" />
            
            <!-- Success Dialog Overlay -->
            <Grid x:Name="DialogOverlay" Visibility="Collapsed" Background="#99000000">
                <materialDesign:Card UniformCornerRadius="20" HorizontalAlignment="Center" VerticalAlignment="Center" Width="400" Padding="32">
                    <StackPanel HorizontalAlignment="Center">
                        <materialDesign:PackIcon Kind="CheckCircle" Foreground="#43A047" Width="80" Height="80" HorizontalAlignment="Center"/>
                        <TextBlock Text="SUCCESS!" FontSize="24" FontWeight="Bold" Margin="0,16,0,8" HorizontalAlignment="Center"/>
                        <TextBlock Text="Your attendance has been recorded." FontSize="16" Opacity="0.7" TextAlignment="Center" Margin="0,0,0,32"/>
                        <Button Content="CONTINUE" Width="150" materialDesign:ButtonAssist.CornerRadius="20" 
                                Command="{Binding CloseDialogCommand}" Click="CloseDialog_Click"/>
                    </StackPanel>
                </materialDesign:Card>
            </Grid>
        </Grid>
    </Grid>
</Window>
