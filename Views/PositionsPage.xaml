<UserControl x:Class="AttendanceShiftingManagement.Views.PositionsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:AttendanceShiftingManagement.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000"
             Background="{StaticResource BrandSurfaceBrush}">
    <UserControl.DataContext>
        <vm:PositionsViewModel/>
    </UserControl.DataContext>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Form Card -->
        <materialDesign:Card Grid.Row="0" Margin="0,0,0,24" Padding="20" UniformCornerRadius="12" Background="White">
            <StackPanel>
                <TextBlock Text="Create New Position" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0,0,0,16" FontWeight="Bold" Foreground="{StaticResource BrandMidnightBrush}"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="120"/>
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Column="0" Margin="0,0,12,0"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             materialDesign:HintAssist.Hint="Position Name (e.g. Production, Service)"
                             Text="{Binding NewName, UpdateSourceTrigger=PropertyChanged}"/>

                    <ComboBox Grid.Column="1" Margin="0,0,12,0"
                              Style="{StaticResource MaterialDesignOutlinedComboBox}"
                              materialDesign:HintAssist.Hint="Area"
                              ItemsSource="{Binding Areas}"
                              SelectedItem="{Binding NewArea}"/>

                    <Button Grid.Column="2" Content="ADD" Height="50"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Background="{StaticResource BrandRedBrush}" Foreground="White"
                            Command="{Binding SaveCommand}"/>
                </Grid>
            </StackPanel>
        </materialDesign:Card>

        <!-- List Card -->
        <materialDesign:Card Grid.Row="1" UniformCornerRadius="12" Padding="0" Background="White">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Background="#FFF8E1" Padding="20,15" BorderBrush="{StaticResource BrandYellowBrush}" BorderThickness="0,0,0,1">
                    <TextBlock Text="Available Positions" FontWeight="Bold" FontSize="16" Foreground="{StaticResource BrandMidnightBrush}"/>
                </Border>

                <ItemsControl Grid.Row="1" ItemsSource="{Binding Positions}" Margin="12">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <materialDesign:Chip Content="{Binding Name}" 
                                                 Margin="4" 
                                                 IsDeletable="True"
                                                 DeleteCommand="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                 DeleteCommandParameter="{Binding}"
                                                 ToolTip="{Binding Area}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </materialDesign:Card>
    </Grid>
</UserControl>
