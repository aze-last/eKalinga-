<Window x:Class="AttendanceShiftingManagement.Views.HolidayDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Holiday Form" 
        Height="400" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Text="{Binding DialogTitle}" 
                   Style="{StaticResource MaterialDesignHeadline5TextBlock}" 
                   FontWeight="Bold" 
                   Margin="0,0,0,24"/>

        <!-- Form -->
        <StackPanel Grid.Row="1">
            <DatePicker materialDesign:HintAssist.Hint="Holiday Date *"
                        SelectedDate="{Binding HolidayDate}"
                        Style="{StaticResource MaterialDesignOutlinedDatePicker}"
                        Margin="0,0,0,16"/>

            <TextBox materialDesign:HintAssist.Hint="Holiday Name *"
                     Text="{Binding HolidayName, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Margin="0,0,0,16"/>

            <CheckBox Content="Double Pay Holiday"
                      IsChecked="{Binding IsDoublePay}"
                      Margin="0,0,0,16"/>

            <TextBlock Text="Note: Employees working on double pay holidays will receive 2x their hourly rate." 
                       TextWrapping="Wrap"
                       Foreground="Gray"
                       FontSize="12"
                       Margin="0,8,0,0"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="CANCEL" 
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Click="Cancel_Click"
                    Margin="0,0,8,0"/>

            <Button Content="SAVE" 
                    Command="{Binding SaveCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>
    </Grid>
</Window>