<UserControl x:Class="AttendanceShiftingManagement.Views.AttendanceLogsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="{StaticResource BrandSurfaceBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{Binding Title}"
                   Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                   FontWeight="Bold"
                   Margin="0,0,0,16"/>

        <materialDesign:Card Grid.Row="1" Padding="16" Background="White">
            <DataGrid ItemsSource="{Binding RowsView}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      GridLinesVisibility="None"
                      Background="Transparent"
                      materialDesign:DataGridAssist.CellPadding="12 8 8 8">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Employee" Binding="{Binding EmployeeName}" Width="*"/>
                    <DataGridTextColumn Header="Position" Binding="{Binding PositionName}" Width="Auto"/>
                    <DataGridTextColumn Header="Shift" Binding="{Binding ShiftTime}" Width="Auto"/>
                    <DataGridTextColumn Header="Time In" Binding="{Binding ClockInTime}" Width="Auto"/>
                    <DataGridTextColumn Header="Time Out" Binding="{Binding ClockOutTime}" Width="Auto"/>
                    <DataGridTemplateColumn Header="Status" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <materialDesign:Chip Content="{Binding Status}" Background="{Binding StatusColor}" Foreground="White" Height="24"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>
    </Grid>
</UserControl>
