<Window x:Class="AttendanceShiftingManagement.Views.DigitalIdPrintPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="eKalinga+ - Digital ID Preview"
        Height="720"
        Width="760"
        MinHeight="620"
        MinWidth="640"
        Background="#F8FAFC"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Digital ID Preview"
                       FontSize="24"
                       FontWeight="SemiBold"
                       Foreground="#0F172A"/>
            <TextBlock Text="Review the beneficiary card layout before sending it to the printer."
                       Margin="0,6,0,0"
                       Foreground="#475569"
                       TextWrapping="Wrap"/>
        </StackPanel>

        <Border Grid.Row="1"
                Margin="0,20,0,20"
                Padding="24"
                Background="White"
                BorderBrush="#CBD5E1"
                BorderThickness="1"
                CornerRadius="20">
            <Viewbox Stretch="Uniform">
                <ContentControl x:Name="PreviewHost"/>
            </Viewbox>
        </Border>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="CLOSE"
                    Width="120"
                    Height="40"
                    Margin="0,0,12,0"
                    Click="CloseButton_Click"/>
            <Button Content="PRINT"
                    Width="140"
                    Height="40"
                    Background="#1D4ED8"
                    Foreground="White"
                    Click="PrintButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
